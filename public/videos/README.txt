Drop your generated videos here with these exact filenames:

  hero-loop.mp4            -> used in components/Hero.tsx
  studio-atmosphere.mp4    -> used in components/StudioAtmosphere.tsx

See the "Video prompts" section of the project README.md for exact specs
and generation prompts for each one. The site works without them (it falls
back to a poster image + gradient) so you can drop them in whenever ready.
